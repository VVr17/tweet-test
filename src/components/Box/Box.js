import styled from "styled-components";
import {
  typography,
  color,
  space,
  layout,
  flexbox,
  grid,
  border,
} from "styled-system";

export const Box = styled("div")(
  typography,
  color,
  space,
  layout,
  flexbox,
  grid,
  border
);
